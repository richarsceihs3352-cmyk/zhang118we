# 警务保障车辆管理系统

本地优先的警务保障车辆管理系统 PWA 应用。

## 📋 环境准备

在开始之前，请确保您的电脑已安装 **Node.js** (推荐 v20 LTS 版本)。
[下载 Node.js](https://nodejs.org/)

安装完成后，在终端运行以下命令检查：
```bash
node -v
npm -v
```

## 🚀 快速开始 (安装与运行)

如果您是刚下载或复制的项目，**必须先安装依赖**才能运行命令。

1. **安装依赖 (必做)**
   推荐使用镜像源加速下载：
   ```bash
   npm install --registry=https://registry.npmmirror.com
   ```
   *如果报错 'vite' 不是内部或外部命令，通常是因为跳过了这一步。*

2. **本地开发启动**
   ```bash
   npm run dev
   ```
   访问: http://localhost:3000

3. **构建生产版本**
   ```bash
   npm run build
   ```
   构建成功后，会生成 `docs` 文件夹（用于 GitHub Pages）或 `dist` 文件夹。

## 🔑 环境变量配置

如果需要使用 AI 功能，请在根目录创建 `.env` 文件并填入：
```env
API_KEY=您的Google_Gemini_API_Key
```
*注意：构建时 API Key 会被打包进代码中，请勿将包含真实 Key 的代码发布到公开仓库。*

## ☁️ 部署指南

### GitHub Pages (推荐)
1. 确保 `vite.config.ts` 中的 `outDir` 设置为 `docs`。
2. 运行 `npm run build`。
3. 将代码推送到 GitHub。
4. 在 GitHub 仓库 Settings -> Pages 中，选择 `main` 分支和 `/docs` 文件夹。

### AppsGeyser (打包 APK)
1. 运行 `npm run build`。
2. 进入生成的文件夹 (`docs` 或 `dist`)。
3. 全选所有文件 -> 压缩为 ZIP。
4. 在 AppsGeyser 上传 ZIP 包。"# zhang118we" 
